{
  "config": {
    "step": {
      "user": {
        "title": "HiveFW HA Integration",
        "description": "Add the HiveFW HA Integration for HiveFW Companion-Repeater management, chat, channels, nodes and diagnostics. The core meshcore integration must also be installed for the panel to receive messages."
      }
    },
    "abort": {
      "single_instance_allowed": "HiveFW HA Integration is already configured.",
      "meshcore_not_installed": "The MeshCore integration is not installed. Install it from HACS first, then add HiveFW."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "HiveFW HA Integration options",
        "description": "Tune how long the message store retains conversation history. Changes take effect on the next message save and at the next Home Assistant restart.",
        "data": {
          "max_messages_per_conversation": "Max messages per conversation",
          "message_retention_days": "Message retention (days)"
        },
        "data_description": {
          "max_messages_per_conversation": "When a conversation exceeds this number, the oldest messages are trimmed first. Range: 50 – 5000. Default: 500.",
          "message_retention_days": "Messages older than this are pruned at startup and during periodic cleanup. Range: 1 – 365. Default: 90."
        }
      }
    }
  },
  "issues": {
    "upstream_meshcore_unavailable": {
      "title": "MeshCore integration is not configured",
      "fix_flow": {
        "step": {
          "confirm": {
            "title": "HiveFW HA Integration needs the MeshCore integration",
            "description": "HiveFW HA Integration uses the core meshcore integration, but no MeshCore device is currently configured. Install and configure the meshcore integration first (Settings → Devices & Services → Add Integration → MeshCore), or remove HiveFW if you no longer need it.\n\nClick Submit to acknowledge this issue. It will clear automatically once a MeshCore device is configured."
          }
        }
      }
    },
    "name_changed": {
      "title": "MeshCore device renamed",
      "description": "The MeshCore device was renamed from `{old_name}` to `{new_name}`. {count} entity IDs were automatically migrated from the `_{old_suffix}` suffix to `_{new_suffix}`.\n\n**Renamed entities:**\n\n{entity_list}\n\nIf you have automations, scripts, or dashboards referencing the old entity IDs, you will need to update them manually to use the new suffix.\n\nThis issue can be dismissed once you have verified your configuration."
    }
  },
  "exceptions": {
    "operation_failed": {
      "message": "Operation failed"
    },
    "device_not_connected": {
      "message": "Device not connected"
    },
    "operation_timed_out": {
      "message": "Operation timed out"
    },
    "invalid_request": {
      "message": "Invalid request parameters"
    },
    "no_meshcore_coordinator": {
      "message": "No active MeshCore coordinator"
    },
    "invalid_key_length": {
      "message": "Private key must be 64 or 128 hex characters"
    },
    "invalid_key_hex": {
      "message": "Private key must contain only hex characters (0-9, a-f)"
    },
    "import_rejected": {
      "message": "The device firmware rejected the new identity. The device's identity is unchanged."
    },
    "verify_failed": {
      "message": "The new identity did not take effect on the device. The device's identity may be unchanged."
    }
  }
}
